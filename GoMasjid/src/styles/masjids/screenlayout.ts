// styles/screenLayout.ts

import { StyleSheet } from 'react-native';

export const screenLayoutStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF',
  },
  scrollContainer: {
    flexGrow: 1,
    paddingBottom: 100,
  },
  
});
